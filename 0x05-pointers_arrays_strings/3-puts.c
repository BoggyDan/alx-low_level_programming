#include "main.h"
/* for puts() */
#include <stdio.h>

/**
 * _puts - print a string followed by a new line
 * @str: pointer to where the string is stored
 *
 */
void _puts(char *str)
{
	puts(str);
}
