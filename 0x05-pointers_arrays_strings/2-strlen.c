#include "main.h"
/* for strlen() */
#include <string.h>

/**
 * _strlen - length of a string
 * @s: pointer to where the string is stored
 *
 * Return: length of string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));
}
