#include <unistd.h>
/* for abs() */
#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes absolute value
 * @n: number of which absolute value is computed
 *
 * Description: A function that computes the absolute value of an integer
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	return (abs(n));
}
